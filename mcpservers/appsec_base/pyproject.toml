[project]
name = "appsec-base-mcp"
version = "0.1.0"
description = "Base MCP Server for Application Security Tools"
requires-python = ">=3.11"
dependencies = [
    "fastmcp>=2.12.5",
    "hd-logging>=1.0.0",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

